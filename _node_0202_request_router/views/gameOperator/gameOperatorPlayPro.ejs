<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /*  text-decoration: none;  ==> a태그의 밑줄 을 없애준다. */
        a {
            text-decoration: none; 
        }

    </style>
</head>
<body>
    <!--
        [연산자게임]
            [1]  (4~9) 사이의 랜덤숫자 두개와 그숫자 로연산한 답이 제시된다. (예시) 3 ? 4 = 12
            [2]  ? 에들어갈 연산자를 고르면된다. 

    -->
    <%
        num1 = parseInt(num1);
        num2 = parseInt(num2);

        result = parseInt(result);
        op = parseInt(op);

        select = parseInt(select);
        selStr = ""

        switch(select){
            case 1: selStr = "+"; break;
            case 2: selStr = "-"; break;
            case 3: selStr = "X"; break;
            default : break;
        }

        gameCount = parseInt(gameCount);
        gameScore = parseInt(gameScore);
    %>
    <h3>연산자 게임</h3>
    <h3><%=gameCount%>번 째 게임 : <%=gameScore%>점</h3>
    <h2>
    <%=num1%> <%=selStr%> <%=num2%> = <%=result %> 
    </h2>

    
    <% if(op == select){%>
        <% gameScore += 10; %>
        <h2>정답입니다</h2>    
    <%}else {%>
        <h2>오답입니다</h2>
    <%}%>

    <% if(gameCount != 5) { %>
        <a href="gameOperatorPlay?gameCount=<%=gameCount+1%>&gameScore=<%=gameScore%>">돌아가기</a>
    <%}else{%>
        <h3>게임 종료</h3>
        <a href="gameOperatorMain">처음으로</a>
    <%}%>
</body>
</html>